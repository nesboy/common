Configuration:
  status: WARN

  appenders:
    Console:
      name: CONSOLE
      JsonLayout:
        compact: true
        eventEol: true
        properties: true
        includeTimeMillis: true

  Loggers:
    Root:
      level: DEBUG
      AppenderRef:
        - ref: CONSOLE
